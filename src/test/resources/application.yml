object-storage:
  vendor-name: aws # aws, oracle
  presigned-url-expire-length: 600000
  profile-image-folder-name: profile-image
  qr-image-folder-name: qr-image



server:
  port: 8888

spring:
  profiles:
    active: test
  data:
    redis:
      host: server
      port: 6379
      password: password
  datasource:
    url: jdbc:oracle:thin:@(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=g2745971f4b4d89_test_tpurgent.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    username: ADMIN
    password: GOCSPX-DE5r6
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: create
  mail:
    host: smtp.gmail.com
    port: 587
    username: test
    password: test
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
google:
  oauth:
    client-id: client_id
    client-secret: client_secret
    redirect-uri: redirect-uri
  api-url:
    access-token: http://localhost:8888/token
    profile: http://localhost:8888/profile

refresh-token:
  # 10초 밀리초 단위로 계산
  # 10 * 1000
  expire-length: 10000

jwt:
  secret-key: secretKeysecretKeysecretKeysecretKeysecretKeysecretKey
  # 10초 밀리초 단위로 계산
  # 10 * 1000
  expire-length: 10000

aws:
  access-key: accessKey
  secret-access-key: secretAccessKey
  s3:
    bucket-name: test-bucket
    region: ap-northeast-2
    end-point: https://s3.ap-northeast-2.amazonaws.com

tag:
  front-domain: test

encryption:
  password: aaaaaaaaaaaaaaaaaaaaaa1234567890
  salt: aaaaaaaaaaaaaaaaaaaaaa1234567890

logging:
  level:
    root: INFO
    sql: trace

